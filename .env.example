MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=topsellrun

# min 32 chars — generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=topsellrun

# URL publik situs (tanpa slash di akhir), dipakai untuk metadataBase / link absolut
NEXT_PUBLIC_SITE_URL=
